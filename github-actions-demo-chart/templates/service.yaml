apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-actions-demo-chart.fullname" . }}
  labels:
    {{- include "github-actions-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-actions-demo-chart.selectorLabels" . | nindent 4 }}
