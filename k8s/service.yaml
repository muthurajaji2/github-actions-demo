apiVersion: v1
kind: Service

metadata:
  name: github-actions-demo-service

spec:
  selector:
    app: github-actions-demo

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: NodePort